diff --git a/CMakeLists.txt b/CMakeLists.txt
index ad37349..7d7fac3 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -33,8 +33,10 @@ target_include_directories(miotts PRIVATE
 target_link_libraries(miotts PRIVATE
     llama
     ggml
-    m
 )
+if(NOT WIN32)
+    target_link_libraries(miotts PRIVATE m)
+endif()
 
 add_executable(miotts-stream-device
     examples/stream-to-device.cpp
@@ -56,8 +58,10 @@ target_include_directories(miotts-stream-device PRIVATE
 target_link_libraries(miotts-stream-device PRIVATE
     llama
     ggml
-    m
 )
+if(NOT WIN32)
+    target_link_libraries(miotts-stream-device PRIVATE m)
+endif()
 
 add_executable(miotts-stream-benchmark
     examples/stream-benchmark.cpp
@@ -78,8 +82,10 @@ target_include_directories(miotts-stream-benchmark PRIVATE
 target_link_libraries(miotts-stream-benchmark PRIVATE
     llama
     ggml
-    m
 )
+if(NOT WIN32)
+    target_link_libraries(miotts-stream-benchmark PRIVATE m)
+endif()
 
 add_executable(miotts-stream-compare
     examples/stream-compare.cpp
@@ -100,5 +106,7 @@ target_include_directories(miotts-stream-compare PRIVATE
 target_link_libraries(miotts-stream-compare PRIVATE
     llama
     ggml
-    m
 )
+if(NOT WIN32)
+    target_link_libraries(miotts-stream-compare PRIVATE m)
+endif()
diff --git a/src/istft.cpp b/src/istft.cpp
index add3f47..c58bb8d 100644
--- a/src/istft.cpp
+++ b/src/istft.cpp
@@ -1,9 +1,14 @@
 #include "istft.h"
 
+#define _USE_MATH_DEFINES
 #include <algorithm>
 #include <cmath>
 #include <vector>
 
+#ifndef M_PI
+#define M_PI 3.14159265358979323846
+#endif
+
 istft_cache::istft_cache(int n_fft, int win_length) {
     n_fft_ = n_fft;
     win_length_ = win_length;
diff --git a/src/main.cpp b/src/main.cpp
index e9befbc..739a555 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -12,10 +12,11 @@ struct miotts_params {
     std::string output_path = "output.wav";
     std::string text;
 
-    float temperature = 0.8f;
+    float temperature = 0.5f;
     int max_tokens = 700;
     int n_threads = 4;
     int n_gpu_layers = 0;
+    int seed = 42;
 
     bool dump_tensors = false;
     bool skip_llm = false;
@@ -31,7 +32,8 @@ static void print_usage(const char * prog) {
             "  -v, --voice PATH       Voice embedding .emb.gguf path (required)\n"
             "  -o, --output PATH      Output WAV file path (default: output.wav)\n"
             "  -p, --prompt TEXT      Text to synthesize (required)\n"
-            "  -t, --temp FLOAT       Sampling temperature (default: 0.8)\n"
+            "  -t, --temp FLOAT       Sampling temperature (default: 0.5)\n"
+            "  --seed N               Sampler seed (default: 42)\n"
             "  --max-tokens N         Max tokens to generate (default: 700)\n"
             "  --threads N            Number of CPU threads (default: 4)\n"
             "  -ngl N                 Number of GPU layers (default: 0)\n"
@@ -64,6 +66,9 @@ static bool parse_args(int argc, char ** argv, miotts_params & params) {
         } else if (arg == "-t" || arg == "--temp") {
             if (++i >= argc) return false;
             params.temperature = std::stof(argv[i]);
+        } else if (arg == "--seed") {
+            if (++i >= argc) return false;
+            params.seed = std::stoi(argv[i]);
         } else if (arg == "--max-tokens") {
             if (++i >= argc) return false;
             params.max_tokens = std::stoi(argv[i]);
@@ -129,6 +134,7 @@ int main(int argc, char ** argv) {
     cfg.n_gpu_layers = params.n_gpu_layers;
     cfg.temperature = params.temperature;
     cfg.max_tokens = params.max_tokens;
+    cfg.seed = (uint32_t)params.seed;
 
     TestToSpeech tts(cfg);
     if (!tts.is_ready()) {
@@ -145,6 +151,7 @@ int main(int argc, char ** argv) {
     TestToSpeech::Options opt;
     opt.temperature = params.temperature;
     opt.max_tokens = params.max_tokens;
+    opt.seed = params.seed;
     opt.skip_llm = params.skip_llm;
 
     if (!tts.synthesize_to_file(voice, params.text, params.output_path, opt)) {
diff --git a/src/test-to-speech.cpp b/src/test-to-speech.cpp
index 4e3e9ed..ab38ab5 100644
--- a/src/test-to-speech.cpp
+++ b/src/test-to-speech.cpp
@@ -88,7 +88,7 @@ int TestToSpeech::sample_rate() const {
 }
 
 std::string TestToSpeech::build_prompt(const std::string & text) {
-    return "<|startoftext|><|im_start|>user\n" + text + "<|im_end|>\n<|im_start|>assistant\n";
+    return "<|im_start|>user\n" + text + "<|im_end|>\n<|im_start|>assistant\n";
 }
 
 std::string TestToSpeech::run_llm(const std::string & text, const Options & options) {
@@ -99,6 +99,7 @@ std::string TestToSpeech::run_llm(const std::string & text, const Options & opti
 
     const float temperature = options.temperature >= 0.0f ? options.temperature : config_.temperature;
     const int max_tokens = options.max_tokens > 0 ? options.max_tokens : config_.max_tokens;
+    const uint32_t seed = options.seed >= 0 ? (uint32_t)options.seed : config_.seed;
 
     llama_context_params ctx_params = llama_context_default_params();
     ctx_params.n_ctx = 2048;
@@ -126,8 +127,10 @@ std::string TestToSpeech::run_llm(const std::string & text, const Options & opti
 
     llama_sampler_chain_params chain_params = llama_sampler_chain_default_params();
     llama_sampler * sampler = llama_sampler_chain_init(chain_params);
+    llama_sampler_chain_add(sampler, llama_sampler_init_top_k(50));
+    llama_sampler_chain_add(sampler, llama_sampler_init_penalties(64, 1.5f, 0.0f, 0.0f));
     llama_sampler_chain_add(sampler, llama_sampler_init_temp(temperature));
-    llama_sampler_chain_add(sampler, llama_sampler_init_dist(42));
+    llama_sampler_chain_add(sampler, llama_sampler_init_dist(seed));
 
     llama_batch batch = llama_batch_init(tokens.size(), 0, 1);
     for (int i = 0; i < n_tokens; i++) {
@@ -434,6 +437,7 @@ bool TestToSpeech::synthesize_stream_profiled(const VoiceModel & voice,
 
     const float temperature = options.temperature >= 0.0f ? options.temperature : config_.temperature;
     const int max_tokens = options.max_tokens > 0 ? options.max_tokens : config_.max_tokens;
+    const uint32_t seed = options.seed >= 0 ? (uint32_t)options.seed : config_.seed;
 
     llama_context_params ctx_params = llama_context_default_params();
     ctx_params.n_ctx = 2048;
@@ -461,8 +465,10 @@ bool TestToSpeech::synthesize_stream_profiled(const VoiceModel & voice,
 
     llama_sampler_chain_params chain_params = llama_sampler_chain_default_params();
     llama_sampler * sampler = llama_sampler_chain_init(chain_params);
+    llama_sampler_chain_add(sampler, llama_sampler_init_top_k(50));
+    llama_sampler_chain_add(sampler, llama_sampler_init_penalties(64, 1.5f, 0.0f, 0.0f));
     llama_sampler_chain_add(sampler, llama_sampler_init_temp(temperature));
-    llama_sampler_chain_add(sampler, llama_sampler_init_dist(42));
+    llama_sampler_chain_add(sampler, llama_sampler_init_dist(seed));
 
     llama_batch batch = llama_batch_init(tokens.size(), 0, 1);
     for (int i = 0; i < n_tokens; i++) {
diff --git a/src/test-to-speech.h b/src/test-to-speech.h
index be47ee2..d508f83 100644
--- a/src/test-to-speech.h
+++ b/src/test-to-speech.h
@@ -42,13 +42,15 @@ public:
         std::string codec_path;
         int n_threads = 4;
         int n_gpu_layers = 0;
-        float temperature = 0.8f;
+        float temperature = 0.5f;
         int max_tokens = 700;
+        uint32_t seed = 42;
     };
 
     struct Options {
         float temperature = -1.0f; // negative => use Config default
         int max_tokens = -1;       // negative => use Config default
+        int seed = -1;             // negative => use Config default
         bool skip_llm = false;     // when true, text is raw <|s_N|> token text
         bool apply_peak_normalization = true;
     };
